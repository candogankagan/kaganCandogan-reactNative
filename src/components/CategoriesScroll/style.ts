import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    itemContainer: {
        alignSelf: 'flex-start',
        borderWidth: 1,
        borderRadius: 6,
        padding: 10,
        marginHorizontal: 10,
    },
    itemText: {
        color: '#fff',
        fontWeight: '600',
    },
});
